{
  "name": "pulsetrade-stock-dashboard",
  "version": "1.0.0",
  "description": "PulseTrade — Real-Time Stock Subscription Dashboard",
  "private": true,
  "scripts": {
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && PORT=3001 CLIENT_URL=http://localhost:5173 npm run dev",
    "frontend": "cd frontend && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "author": "Shriniwas Maheshwari",
  "license": "MIT"
}
